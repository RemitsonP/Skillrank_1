#include <stdio.h>
int main() {
    int a, b;
    printf("Enter two Integers: ");
    scanf("%d %d", &a, &b);
    a = a + b;
    b = a - b;
    a = a - b;
    printf("After Swapping: %d %d\n", a, b);
    return 0;
}
